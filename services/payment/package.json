{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "ShopFlow payment microservice",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node -e \"const h=require('http');const r=h.request({hostname:'localhost',port:process.env.PORT||3002,path:'/health'},res=>{process.exit(res.statusCode===200?0:1)});r.on('error',()=>process.exit(0));r.end();\""
  },
  "engines": { "node": ">=20.0.0" }
}
